#ifndef PAIR_H
#define PAIR_H

#include <cstring>

// Structure to store file name and offset
struct pair {
    char name[20];    // File name
    int offset;       // File offset
};

#endif // PAIR_H
